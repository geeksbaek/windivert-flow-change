# 모든(*) 파일을 무시함
*

# 모든(*) 디렉터리(/)를 무시하지 않음
!*/

# 파일이름이 .cpp 로 끝나는 모든(*) 파일을 무시하지 않음
!*.cpp

# 파일이름이 .sln 으로 끝나는 모든(*) 파일을 무시하지 않음
!*.sln

# 파일이름이 .vcxproj 로 끝나는 모든(*) 파일을 무시하지 않음
!*.vcxproj

# .gitignore 파일은 블랙리스트의 개념이기 때문에, 화이트리스트처럼 사용하려면
# 위 방법과 같이 모든 파일을 블랙리스트에 추가한 뒤에 화이트리스트로 등록할
# 파일만 블랙리스트에서 따로 빼주어야 합니다.

# #으로 시작하는 라인은 주석입니다.
